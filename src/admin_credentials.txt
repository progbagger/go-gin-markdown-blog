Posting access:
username: root
password: root

Database defaults:
username: postgres
password: postgres
database: postgres
host: localhost
port: 5432

However, DB credentials can be configured via environment variables:
POSTGRES_USER
POSTGRES_PASSWORD
POSTGRES_DATABASE
POSTGRES_HOST
POSTGRES_PORT
